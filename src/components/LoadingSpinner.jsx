function LoadingSpinner({ className }) {
    return <div className={`w-12 h-12 rounded-full animate-spin border-8 border-dashed border-t-transparent ${className}`}></div>
}
export default LoadingSpinner;